# M254.9 Fragment-Present Token Fallback Retirement Execution Review Loop Prompt

Execute this prompt only when `docs/agent/current-redesign-state.md` points
here and records M254.8 as accepted.

This is an implementation/consolidation task. Use the executor-review loop:

```text
single write-capable executor
-> read-only reviewer/auditor subagents
-> focused revision executor if `Needs Revision`
-> focused re-review
-> next-run prompt generation
```

The orchestrator owns final state and next-prompt updates.

## Accepted State

Accepted through:

```text
Milestone 254.8: Fragment-First Lowerer Compatibility Body Retirement
```

M254.1-M254.8 migrated accepted source-body discovery families to prefer
`Implementation.source_body_fragments`, cleaned selected-discovery APIs to be
context-first, removed `ImplementationBody` imports/signatures/construction
from `lowerer.py`, and removed the old
`unsupported_primitive_call_diagnostics(body, ...)` wrapper from
`primitive_calls.py`.

Architecture review for M254.8 accepted the slice with one follow-up:
`_selected_body_token_view(...)` still lets fragment-present selected
implementations fall back to `selected.implementation.body.tokens` when
fragment-derived tokens do not preserve a direct-body shape. That was useful
while fragment ownership was young, but it now weakens ADR-073 because stale
compatibility tokens can override accepted fragments.

## Read First Files

Read these files before planning or editing:

- `AGENTS.md`
- `PLANS.md`
- `docs/agent/current-redesign-state.md`
- `docs/agent/next-run-prompt-protocol.md`
- `docs/redesign/implementation-roadmap.md`
- `docs/redesign/behavioral-spec.md`
- `docs/redesign/design-decisions.md`
- `docs/agent/m254.8-implementationbody-accounting.txt`
- `tslgen/src/tslgen/lowering/lowerer.py`
- `tslgen/src/tslgen/lowering/source_body_fragments.py`
- `tslgen/src/tslgen/lowering/primitive_calls.py`
- `tslgen/src/tslgen/domain/catalog.py`
- `tslgen/tests/test_m254_5_fragment_first_direct_lowerer_body_consumption.py`
- `tslgen/tests/test_m254_6_fragment_first_primitive_call_inventory.py`
- `tslgen/tests/test_m254_8_fragment_first_lowerer_compatibility_body_retirement.py`

## Goal

Continue the M254.x `ImplementationBody` removal series by retiring the
fragment-present stale-token fallback in direct selected-body lowering.

When `Implementation.source_body_fragments` is present, direct lowering should
consume fragment-derived tokens or report their diagnostics. Compatibility
`implementation.body.tokens` may remain as token-only fallback only for
selected implementations without fragments.

## Scope

Implement the smallest coherent fallback-retirement slice:

- In `lowerer.py`, remove the shape-preservation fallback that returns
  compatibility body tokens when fragments are present.
- Delete now-unused helper functions such as
  `_direct_body_fragment_tokens_preserve_shape(...)` and
  `_has_direct_body_keyword(...)` if they become dead.
- Preserve token-only fallback for selected implementations where
  `source_body_fragments is None`.
- Add a stale-token preference regression proving a fragment-present selected
  implementation is lowered from fragments even if its compatibility
  `body.tokens` contain a conflicting lowerable operation or directive.
- Keep M254.8 guardrails proving `lowerer.py` and `primitive_calls.py` do not
  import, construct, or expose `ImplementationBody`.
- Record exact remaining `ImplementationBody` references and use that
  accounting to select the next M254.x prompt.

## Non-Negotiable Guardrails

- No new TSIL keyword semantics or keyword spellings.
- No new parser, source scanner, or source repair path.
- No pairwise surrounding-keyword handlers.
- No backend rendering, generated-project build verification, template
  rendering, CLI workflow, primitive-call selector semantics, dependency
  closure semantics, assignment/index parsing, or target-language expression
  parsing.
- Do not remove the domain `ImplementationBody` class, catalog-builder
  compatibility construction, primitive-project compatibility construction, or
  token-only tests in this milestone.
- Do not introduce runtime dependencies on `frozen` or `tslgenold`.
- Do not reactivate M255 while production body lowering still depends on
  compatibility body/token fields.

## Expected Tests

Add or update focused tests, likely in:

```text
tslgen/tests/test_m254_9_fragment_present_token_fallback_retirement.py
```

Cover:

- Fragment-present direct body lowering ignores conflicting compatibility
  body tokens and follows the fragment source.
- Existing token-only fallback tests continue to pass when fragments are absent.
- Static guardrails assert the removed fallback helper names do not remain in
  `lowerer.py`, and M254.8 `ImplementationBody` guardrails still hold.

## Out Of Scope

Changing lowering semantics beyond choosing fragments whenever fragments are
present, removing the domain `ImplementationBody` class, removing
catalog/pipeline compatibility body construction, deleting token-only tests,
backend rendering, generated-project build verification, primitive-call
selector semantics, dependency-closure behavior, source repair,
target-language parsing, fixture-shaped pipelines, runtime dependencies on
`frozen` or `tslgenold`, and M255.

If evidence shows this cannot be done safely in one coherent slice, stop
before speculative implementation and create a narrower M254.9 planning prompt.

## Required Validation

Run:

```bash
git diff --check
python -B -m compileall -q tslgen/src/tslgen tslgen/tests
PYTHONPATH=tslgen/src python -B -m pytest -p no:cacheprovider tslgen/tests/test_m107_tiny_pipeline.py tslgen/tests/test_m254_5_fragment_first_direct_lowerer_body_consumption.py tslgen/tests/test_m254_8_fragment_first_lowerer_compatibility_body_retirement.py tslgen/tests/test_m254_9_fragment_present_token_fallback_retirement.py
rg -n "\bImplementationBody\b" tslgen/src tslgen/tests
find tslgen -type d -name __pycache__ -print
```

The `rg` command remains an accounting command. Report its exact output and
use it to select the next M254.x step. If validation creates `__pycache__`
directories under `tslgen`, remove them and rerun the final `find` command.
Also remove local `.pytest_cache`, `.mypy_cache`, or `.ruff_cache`
directories if validation creates them.

## Required Review/Audit Subagents

After implementation and validation, run read-only subagents:

1. Architecture/boundary reviewer: fragment-present direct lowering can no
   longer be overridden by compatibility body tokens; no new parser/source
   scanner, pairwise keyword paths, semantic broadening, or increased
   `ImplementationBody` centrality.
2. Evidence reviewer: removed fallback helpers are gone, token-only fallback
   remains only for fragment-absent selections, and remaining
   `ImplementationBody` references are accurately classified.
3. Test reviewer: stale-token preference regression, token-only fallback
   coverage, and static guardrails.
4. Documentation reviewer: roadmap/state/spec consistency and next M254.x
   prompt accuracy.
5. Validation auditor: exact validation results, `rg` accounting output, and
   workspace hygiene.

If any reviewer returns `Needs Revision`, make only focused fixes for the
blocking issues and rerun the relevant focused review. If any returns
`Return To Planner` or `Reject`, stop implementation and create the appropriate
next prompt.

## Completion Rules

Before finishing:

- update `docs/redesign/implementation-roadmap.md` with the M254.9 result;
- update `docs/redesign/behavioral-spec.md` or
  `docs/redesign/design-decisions.md` if behavior or architecture changes;
- update `docs/agent/current-redesign-state.md`;
- create the next concrete prompt under `docs/agent/runs/`;
- select another M254.x prompt unless production/test `ImplementationBody`
  references have been fully removed;
- do not reactivate or start M255 while production body lowering still depends
  on compatibility body/token fields.

## Final Report

Report:

1. Implementation summary.
2. Which fragment-present fallback path was retired.
3. Which `ImplementationBody` references remain, with exact accounting output.
4. How accepted token-only fallback and diagnostics were preserved.
5. Review/audit verdicts.
6. Validation commands and exact results.
7. Next active M254.x prompt path.
