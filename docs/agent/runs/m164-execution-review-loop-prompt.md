# M164 Execution Review Loop Prompt

This is the active follow-on prompt after M163. Execute it only when
`docs/agent/current-redesign-state.md` points here and records M163 as
accepted.

You are executing and reviewing the accepted next milestone after M163:

```text
Milestone 164: Exact Backend Value Query Request Boundary
```

Milestones 1 through 163 are accepted. M163 records exact top-level
`var<...>(...)` declaration directives as unresolved backend-facing
declaration requests and preserves explicit type/initializer payload text.

M164 is an implementation milestone. It should add the next backend-owned
lowering boundary for exact `value<backend>(...)` islands. These islands are
important for lowering, but they are normally not solvable at generation time:
M164 records source-owned backend value query requests and preserves
surrounding text. It must not evaluate backend values, choose backend
spellings, render code, or infer semantics from the payload text.

## Read First

- `docs/agent/current-redesign-state.md`
- `AGENTS.md`
- `PLANS.md`
- `docs/agent/next-run-prompt-protocol.md`
- `docs/agent/review-checklist.md`
- `docs/redesign/kiss-generator-restart.md`
- `docs/redesign/requirements.md`
- `docs/redesign/implementation-roadmap.md`
- `docs/redesign/behavioral-spec.md`
- `docs/redesign/domain-model.md`
- `docs/redesign/design-decisions.md`
- `docs/redesign/target-architecture.md`
- `docs/redesign/pipeline-design.md`
- `docs/redesign/testing-strategy.md`
- `docs/redesign/tsil-surface-inventory.md`
- `docs/redesign/missing-lowering-inventory.md`
- `tslgen/src/tslgen/syntax/tsil_lexical.py`
- `tslgen/src/tslgen/lowering/model.py`
- `tslgen/src/tslgen/lowering/lowerer.py`
- `tslgen/src/tslgen/lowering/generation_variables.py`
- `tslgen/tests/test_m107_tiny_pipeline.py`
- `tslgen/tests/test_m1625_tsil_lexical.py`
- `tslgen/tests/test_m163_generation_variables.py`

## Goal

Recognize exact backend value query islands in source-owned text:

```text
OPAQUE_TEXT value<backend>(QUERY_TEXT) OPAQUE_TEXT
```

The result should record source-owned non-query text spans and unresolved
backend value query requests in source order, or the equivalent minimal typed
placement/slice contract needed to preserve source identity and diagnostics.
`QUERY_TEXT` remains opaque backend-owned text for later backend translation.

The accepted shape is the `value<backend>(...)` island itself, not any
surrounding corpus pattern such as `intrin_compose<..., suffix=...>` or
`var<typed>(..., value<backend>(uninit::array))`.

## Required Executor Task

Run exactly one write-capable executor for M164. The executor should:

1. Inspect dirty worktree state before editing and preserve unrelated changes.
2. Inventory current `value<backend>(...)` payload forms across all
   `tsldata/**/*.tsl` files. Use corpus examples as evidence, not as accepted
   surrounding-shape templates.
3. Add the smallest exact backend value query island boundary over
   source-owned text that can identify exact `value<backend>(...)` islands,
   preserve source-owned non-query text spans, and carry unresolved query
   payload text forward for later backend-specific handling.
4. Provide a body-token discovery entry point over `ImplementationBody.tokens`
   and a text-fragment discovery helper usable by current opaque payload
   carriers such as M163 `GenerationVariableDeclarationText`, without adding a
   broad expression parser or context-specific consumer for each possible
   occurrence.
5. Preserve query payload text as opaque. Nested `type<generation>(...)`,
   `type<backend>(...)`, quoted strings, intrinsic names, backend keys,
   casts, primitive calls, operators, and helper calls inside payloads are not
   interpreted by M164.
6. Match only balanced outer `value<backend>(...)` islands. Use the M162.5
   shared lexical helper where it directly fits. Keep any query-specific
   diagnostics local.
7. Preserve all non-query text/tokens as source-owned opaque spans. Loops,
   generation branches, returns, raw helper calls, assignments, array
   indexing, casts, intrinsics, primitive calls, backend-control directives,
   declarations, and `type<backend>(...)` islands outside a `value<backend>`
   payload are not interpreted by M164.
8. Emit deterministic diagnostics for malformed outer query islands and no
   exact backend value query when the caller explicitly asks for one.
9. Preserve M155-M163 accepted behavior, diagnostics, source locations,
   selected-branch handoff, helper raw preservation, loop discovery behavior,
   declaration request behavior, and generated bytes.
10. Add focused tests for:
    - `value<backend>(uninit::array)` and `value<backend>(uninit::scalar)`;
    - intrinsic forms such as `value<backend>(intrin::suffix(...))`,
      `value<backend>(intrin::suffix)`, and `value<backend>(intrin::prefix)`;
    - nested delimiter payload preservation without backend evaluation;
    - source text with arbitrary opaque prefix, suffix, and multiple query
      islands in source order;
    - discovery over body tokens and over an M163-style declaration text
      fragment;
    - malformed outer query diagnostics;
    - no-query diagnostics;
    - determinism.
11. Update docs that describe the accepted M164 behavior and any newly
    discovered boundary details.
12. Leave final accepted-state updates to the orchestrator after read-only
    review returns `Accept` or `Accept With Follow-Ups`.

## Design Guardrails

- Treat this as lexical island discovery and unresolved backend value query
  request intake over source-owned text, not as backend value evaluation.
- The payload of `value<backend>(...)` is backend-owned opaque text in M164.
- Do not solve `intrin::suffix`, `intrin::prefix`, `uninit::*`, x86 constants,
  type spellings, backend helper names, target-language expressions, or
  declaration initializers.
- Do not render backend text, infer types, resolve symbols, lower
  `type<backend>(...)` outside existing accepted type-query paths, lower
  `intrin_compose<...>`, parse expressions/statements, execute loops,
  substitute variables, schedule dependencies, read `tsldata`, `frozen`, or
  `tslgenold` at runtime, or add broad registries, dispatchers, worklists,
  callback maps, hidden backfeeds, or fixpoint machinery.

## Must Preserve

- M107-M163 accepted behavior, diagnostics, source locations, and generated
  bytes.
- M155 isolated generation-value query behavior.
- M156-M160 generation-control region and branch-chain behavior.
- M161 whole-body exact loop-region fact behavior.
- M162 embedded loop-region discovery behavior.
- M162.5 shared lexical-helper behavior and migrated keyword/classifier
  boundaries.
- M163 exact top-level generation variable declaration request discovery.
- Accepted primitive-call resolver/collector behavior and helper raw
  preservation.

## Out Of Scope

Backend value translation/results; backend-specific spelling; rendering;
`type<backend>(...)` broad discovery; declaration rendering; type inference;
symbol tables; initializer expression evaluation; recursive lowering of query
payloads; `let<...>` lowering; loop execution or unrolling; loop-variable
substitution; assignment, array-access, cast, memory, I/O, intrinsic,
primitive-call, backend-control, or backend rendering; source repair;
dependency scheduling; output writing; runtime `tsldata`, `frozen`, or
`tslgenold` dependencies; broad registries, dispatchers, worklists, callback
maps, hidden backfeeds, or fixpoint mechanisms.

## Required Review/Audit Subagents

After the executor finishes, use read-only subagents:

1. Architecture reviewer: verify M164 adds only exact unresolved
   `value<backend>(...)` request facts over source-owned text and avoids
   backend evaluation, rendering, expression parsing, symbol tables,
   surrounding-token special cases, registries, dispatchers, worklists, source
   repair, and runtime data reads.
2. Boundary auditor: verify M155-M163 behavior remains intact, query payloads
   remain opaque, and existing type-query/primitive-call/declaration behavior
   is not widened accidentally.
3. Evidence auditor: verify the accepted backend value query surface is
   grounded in current `tsldata/**/*.tsl` evidence without treating corpus
   neighbor patterns as accepted shapes.
4. Test auditor: verify tests cover payload families, nested delimiter
   payloads, opacity, multiple islands, body-token and declaration-text use,
   diagnostics, and determinism.
5. Documentation auditor: verify roadmap, behavioral/domain docs, missing
   inventory, TSIL surface inventory, and current state are coherent.
6. Validation auditor: verify required validation ran and report exact command
   results.

Reviewer/auditor subagents are read-only. They must not edit files.

## Required Validation

Run:

```bash
git diff --check
python -B -m compileall -q tslgen/src/tslgen tslgen/tests/test_m107_tiny_pipeline.py tslgen/tests/test_m1625_tsil_lexical.py tslgen/tests/test_m163_generation_variables.py
PYTHONPATH=tslgen/src python -B -m pytest -p no:cacheprovider tslgen/tests/test_m107_tiny_pipeline.py tslgen/tests/test_m1625_tsil_lexical.py tslgen/tests/test_m163_generation_variables.py
find tslgen -type d -name __pycache__ -print
```

If the executor adds focused M164 test files, include them in the compileall
and targeted pytest commands. Remove validation-created `__pycache__`
directories before the final cache check if any are created.

## Completion Rules

If M164 review returns `Accept` or `Accept With Follow-Ups`:

- update `docs/agent/current-redesign-state.md`;
- mark M164 accepted in `docs/redesign/implementation-roadmap.md`;
- record follow-ups in state if any;
- create the next concrete run prompt under `docs/agent/runs/`.

To keep planning and execution integrated, do the next-run planning inside
this prompt after M164 is accepted. Do not start M165 until the next prompt
explicitly selects it.

If review returns `Needs Revision`, run one focused revision executor and then
a focused re-review. If review returns `Return To Planner` or `Reject`, stop
implementation and create the appropriate planner/rollback prompt instead of
continuing.

Do not start Milestone 165 in this prompt.

## Final Report

Report:

1. Files/directories changed.
2. Executor summary.
3. Review/audit verdicts and any follow-ups.
4. Validation commands and exact results.
5. Next active prompt path.
