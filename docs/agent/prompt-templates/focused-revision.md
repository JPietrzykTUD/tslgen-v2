# Focused Revision Template

Read `docs/agent/current-redesign-state.md` and the prior review result first.

Fix only the blocking issues identified by the reviewer.

Do not broaden scope. Do not start a new milestone.

Run the validation commands requested by the focused revision prompt and report
exact results.
