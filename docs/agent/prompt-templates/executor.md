# Executor Template

Read `docs/agent/current-redesign-state.md`, `AGENTS.md`, and `PLANS.md` first.

Implement exactly the selected milestone or focused revision.

Do not start future milestones.

Use typed models, explicit diagnostics, deterministic ordering, and documented
pipeline boundaries. Do not use `frozen/` as runtime input.

Return the standard review packet from `PLANS.md`.
