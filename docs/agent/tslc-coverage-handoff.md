# TSLc Primitive Coverage — Handoff

Date: 2026-06-20. Audience: a Claude agent continuing the primitive build-coverage
work in a fresh session. Read this top-to-bottom once; it captures everything
non-obvious.

## TL;DR

- The active codebase is **`tslc/`** (compiles `tsldata/**.tsl` → C++ and Rust SIMD
  code). `frozen/` is the old working generator (behavioral oracle); `tslgen/`/
  `tslgenold/` are orientation only — never imported at runtime.
- We are running a **build-coverage campaign**: make every vectorizable primitive
  actually *compile* in C++ and Rust, tracked by a coverage table.
- **Current state: 83 / 89 primitives build-verified.** All work is committed
  (clean tree). Latest commit `d8ff0a1`.
- Coverage source of truth: **`docs/redesign/primitive-coverage-inventory.md`**,
  regenerated by `python tslc/tools/coverage_inventory.py`.
- Remaining: `memory_cp` (needs a `mem` keyword) + 5 NONE primitives
  (`allocate`/`allocate_aligned`/`deallocate` non-vector, `set` variadic,
  `to_ostream`). See "Remaining work".

## Orientation (read these first)

- `tslc/CHARTER.md` — the design rules. **Follow them.** Key ones: grow by
  *vocabulary* not *plumbing*; a new type/helper earns its place only when it lets
  a primitive compile that couldn't before; body model is a `Segment` sequence
  (raw text + keyword regions), not an AST — raw text passes through verbatim, only
  keyword islands are lowered; structured diagnostics never `SystemExit`; pure
  stages, I/O only at load/write; deterministic ordering.
- `tslc/README.md` — pipeline overview: `sources → parse → catalog → select →
  scan body → lower → backend → render → write → verify`.
- `docs/agent/README.md` — orientation map for `docs/agent/`.
- `docs/agent/tslc-vector-query-handoff.md` — the earlier (vector-query / lowering
  modularization) handoff. Still useful for lowering internals.
- `docs/redesign/design-decisions.md` — ADRs (074 vector queries, 075 diagnostics).

## What the coverage campaign has done (commits, newest first)

- `d8ff0a1` — `lzc`/`lzc_imask`/`lzc_scalar` (+`details::clz` helper). 80→83.
- `ee53ab3` — universal `mask_test` mask-model helper + fixed a `compress` VBMI2
  gating bug. 74→80 (the masked-memory family).
- `47c8fbb` — first wave of build tests + Rust codegen fixes (`arith_rem`,
  Rust keyword escaping `r#mod`, var-decl `var<typed>`, `tzc`/`ctz`). 59→74.
- `b8270a1` — orientation refresh + the coverage inventory tooling + a lowering
  refactor (`Lowerer.lower()` decomposition).

Net: **59 → 83 build-verified.**

## The core working loop (how coverage gets made)

This is the repeatable method. Use it.

1. **Pick a primitive (or family) that lowers but isn't build-verified.** Find them
   in the inventory's "Lower but not build-verified" / "partial" tiers.
2. **Probe lowering first (fast, no compile):**
   ```python
   PYTHONPATH=tslc/src python -c "
   from pathlib import Path; from tslc.api import generate_project
   r=generate_project([Path('tsldata')],
     machine_profiles_path=Path('supplementary/buildsystem/machine_profiles.json'),
     primitives=['NAME'], profiles=['scalar','sse2','avx2','skylake','icelake-rockerlake'])
   print('errors', any(d.severity=='error' for d in r.diagnostics))
   from collections import Counter
   print('emitted', Counter((e.primitive,e.extension) for e in r.coverage))
   print('skips', Counter(s.reason[:60] for s in r.skipped))"
   ```
   Pick profiles where the primitive actually emits a non-empty set.
3. **Add a build test** in `tslc/tests/test_build_verify.py` (copy the boilerplate;
   group by family so the call-graph closes — the pipeline pulls callees via
   dependency closure automatically). Comment what builds and what skips cleanly.
4. **Run it** (see "Running tests" — serial, single session!). The build either
   passes (promote to verified) or surfaces a **real codegen bug**.
5. **Fix the bug**, re-run, repeat. Then run the regression set (any verified test
   touching files you changed), regenerate the inventory, run the non-build suite.

### Where bugs live, by symptom (from experience)

- **`details::FOO` undefined (C++) / `details::FOO` is invalid (Rust)** → a
  scalar-core helper isn't ported. Add it to BOTH `tslc/src/tslc/backend/assets/
  tsl_core.hpp` (template fn) and `tsl_core.rs` (trait + `details::` fn). Port
  semantics from `frozen/supplementary/cpp/runtime_support.hpp`. Helpers added so
  far: `arith_add/mul/rem`, `popcount`, `ctz`, `clz`, `mask_test`. Charter rule:
  only add a helper when a primitive needs it.
- **Rust emits C++ syntax** (e.g. `i8 result = (0 as i8);`, `mem<copy>(...)`,
  `name<Vec>(...)`) → the `.tsl` body used a **raw C++ construct** instead of a
  backend-neutral TSIL region. Fix the *source*: `type<gen>(T) x = v;` →
  `var<typed>(type<gen>(T), x, v)`; `details::mask_test<Vec>(m,i)` → `mask<test>(m,i)`.
  Raw text passes through verbatim (per charter), so C++-only spellings break Rust.
- **Rust "comparison operators cannot be chained" on `foo<bar>`** → a keyword that
  isn't scanned leaks as raw text (`<` parsed as less-than). Either it should be a
  region or (if a Rust identifier) needs escaping.
- **Rust type mismatch returning a count** (e.g. `details::ctz(...)` returned where
  `BaseType` expected) → C++ converts implicitly, Rust doesn't. Wrap the body in
  `cast<static>(type<generation>(base::in), ...)`.
- **`<primitive>` is a Rust keyword** (e.g. `mod`) → handled centrally now by
  `rust_raw_identifier` in `tslc/src/tslc/backend/rust_translation.py` (emits
  `r#mod`), applied at the wrapper name and `render_call`.
- **Body wrongly selected on a profile lacking a feature** → a `requires` /
  type-group grouping bug in the `.tsl` (see the `compress` VBMI2 case below).

## Reference: the mask model (needed for any masked/bit primitive)

Masks have two *physical* representations that the `lane_bitmask` repr_kind
conflated:
- **integer bitset** — generic vector (`u64`), avx512 (`__mmaskN`/`uN`), scalar
  (`bool`). Test lane i with `(mask >> i) & 1`.
- **register lane-mask** — sse/avx2: the mask IS a data register
  (`__m128i`/`__m256i`), each lane all-ones/all-zeros. Test lane i by extracting
  its base-sized chunk and checking nonzero.

`mask<test>(m, i)` lowers per repr (`tslc/src/tslc/lower/region_handlers/masks.py`):
the lowerer remaps `lane_bitmask` + `vector_bits > 0` → a `lane_register` key.
- bitset → inline template `mask_test_lane_bitmask` (`(m>>i)&1`) in
  `tsldata/detail/lang/translate_{cpp,rust}.tsl`.
- register → `mask_test_lane_register` template → the universal helper
  `tsl::details::mask_test<Vec>` (C++ `if constexpr is_integral`) /
  `Self::mask_lane_test` (Rust default method on `SimdVector`, byte-chunk).

Per-extension facts: `scalar` bool/vb0, `generic` lane_bitmask/vb0, `sse`/`avx2`
lane_bitmask/vb128-256 (→register), `sse_vl`/`avx2_vl`/`avx512`
native_predicate_by_lanes (→`__mmaskN` integer). Check with a tiny script over
`catalog.extensions[name].mask_policy.kind` / `.vector_bits`.

## Remaining work (6 primitives)

1. **`memory_cp`** (`void:=(ptr,ptr,s,s)`, in `tsldata/primitives/memory/copy.tsl`).
   Body uses `mem<copy>(...)` and `void *` casts. `mem` is **not a scanned TSIL
   keyword** (`tslc/src/tslc/ir/scan.py` `KEYWORDS`), so it leaks as raw text and
   breaks Rust. C++ has a `mem_copy` translate template; **Rust has none**. Needs:
   add `mem` to the scanner keyword set + a region handler (dispatch
   copy/set/alloc/free to `mem_*` templates), add Rust `mem_*` templates
   (`core::ptr::copy_nonoverlapping` etc.), and handle `void`/`void const *`
   pointer types for Rust (no `void` — use `u8`/`c_void`). Also touches `allocate`/
   `deallocate` eventually. Moderate; 1 primitive of immediate value.

2. **5 NONE primitives** — low value, mostly need new signature-kind support:
   - `allocate`/`allocate_aligned`/`deallocate` — `ptr:=(s)` etc., **no vector
     axis**, so the per-type selector makes no slots. Need a non-vector codegen
     path (host helpers). Likely tied to the `mem` work.
   - `set` (`v:=s...`) — variadic scalar-pack kind `s...`, an unsupported signature
     kind (`_SUPPORTED_KINDS` in `tslc/src/tslc/lower/lowerer.py`).
   - `to_ostream` (`o:=(o,v,s)`) — ostream kind `o`, unsupported signature kind.

3. **Deferred slots within verified primitives** (skips, not failures; lower the
   skip counts but each primitive is already "verified" for its emitted slots):
   - generic-vector representation-change for `cast`/`reinterpret` (~1.5k skips).
   - unresolved `type<generation>` / `value<generation>` queries: `vector::offset_base`
     (now unused after the lzc fix — was vestigial), `vector::mask_underlying_t`,
     `vector::transform`, `type::size_bytes`, `select`, `x86::mm_fround_to_zero`.
     These gate the float `lzc_scalar`, store_mask unpacked paths, div/mod float
     rounding, etc. Implement as new `QueryFunction` classes in
     `tslc/src/tslc/lower/queries.py` (one class + one registry entry each).
   - `mem<copy>` appears in several bodies (hor neon, lzc_scalar float) — the `mem`
     keyword work unblocks those too.

## Critical environment gotchas (WILL bite you)

- **Never write under `/tmp`.** WSL: `/tmp` is the container overlay (VHDX only
  grows). All scratch/output goes under **`./tslctmp`** (workspace 9p mount). The
  harness scratchpad path is under `/tmp` — do NOT use it; use `./tslctmp`.
- **Run pytest serially — one session at a time.** Both `pytest.ini` and
  `tslc/pyproject.toml` fix `--basetemp` under `tslctmp/`. Two concurrent pytest
  sessions (or an `rm -rf tslctmp/...` during a run) **wipe each other's basetemp
  mid-run** → spurious `FileNotFoundError`/`Directory not empty`/"nothing
  verified". **This is exactly the `verify.sh` `FileNotFoundError: ...
  tslctmp/pytest_build_verify` symptom** — the basetemp vanished mid-run. Mitigate:
  `rm -rf tslctmp/pytest*` only when nothing is running; don't run `verify.sh`
  alongside another pytest; `mkdir -p tslctmp` defensively.
- **Toolchain**: build verification needs `cmake` + `zig c++` (`CC=zig cc`,
  `CXX=zig c++`) and `cargo`. It needs a **writable `/root/.cache/zig`** (was
  read-only in earlier sessions; fine now). Backends with a missing tool are
  skipped, not failed.
- **`PYTHONPATH`**: `tslc` tests resolve via their own conftest, but running tslc
  modules/scripts directly needs `PYTHONPATH=tslc/src` (the root `pytest.ini` now
  points at `tslc/src`; a bare `pytest` at the repo root runs the tslc suite).
- Build tests are slow (~30–60s each; the full file is many minutes). Run targeted
  subsets in the background and poll.

## Running tests / verifying

```bash
# fast gate after any change (lowering/render/etc., no compile) — expect 80 passed
python -m pytest -q -k 'not build' tslc/tests

# one build test (serial!)
rm -rf tslctmp/pytest && python -m pytest -q \
  tslc/tests/test_build_verify.py::test_leading_zeros_build

# regenerate the coverage inventory (drives the generator; ~1 min)
python tslc/tools/coverage_inventory.py   # writes docs/redesign/primitive-coverage-inventory.md

# whitespace
git diff --check
```

After adding/fixing a primitive: run its build test + the regression set (every
verified build test whose `.tsl` files you touched), then the non-build suite,
then regenerate the inventory and confirm the verified count rose.

## Key files

- Build tests + the coverage truth: `tslc/tests/test_build_verify.py`,
  `tslc/tools/coverage_inventory.py`, `docs/redesign/primitive-coverage-inventory.md`.
- Scalar-core helpers (port from `frozen/`): `tslc/src/tslc/backend/assets/
  tsl_core.hpp`, `tsl_core.rs`. Static x86 traits: `tsl_x86_traits.hpp`.
- Backend dialects: `tslc/src/tslc/backend/{cpp,rust}_translation.py` (e.g.
  `rust_raw_identifier`), `cpp.py`, `rust.py`.
- Lowering: `tslc/src/tslc/lower/lowerer.py` (`_SUPPORTED_KINDS`),
  `lower/region_handlers/*` (masks.py, declarations.py, calls.py, …),
  `lower/queries.py` (add `QueryFunction`s here), `ir/scan.py` (`KEYWORDS`).
- Translate templates (per-backend spellings): `tsldata/detail/lang/translate_{cpp,rust}.tsl`.
- Selection / requires gating: `tslc/src/tslc/select/selector.py`,
  `tslc/src/tslc/catalog/builder.py` (`_requirements`/`_clauses_from_child`),
  type groups in `tsldata/detail/types.tsl`.

## Worked example: the `compress` VBMI2 bug (selector/requires gotcha)

`compress`'s `[sse, sse_vl]` block grouped `[?i16, ?i32, ?i64]` under one
`requires [avx512f, avx512vl, sse, sse2]`, but `_mm_maskz_compress_epi16` needs
**AVX512_VBMI2** (only epi32/epi64 are F+VL). On Skylake-X (has avx512vl, lacks
vbmi2) the i16 body was wrongly selected → compile error. Fix: split `?i16` (with
`avx512_vbmi2`) from `[?i32, ?i64]`, mirroring the already-correct `[avx2,
avx2_vl]` block. Lesson: when one type in a grouped selector needs extra features,
split it out; check intrinsic feature requirements per type.

## Charter reminders (don't regress on these)

- Behavior-preserving refactors only unless adding real capability.
- No helper/type without a primitive that needs it (e.g. don't add a
  `vector::offset_base` query — it was vestigial; we dropped the dummy arg instead).
- `.tsl` expresses backend-neutral intent; raw C++/Rust spellings in bodies are
  bugs (they only work for one backend).
- Keep C++/Rust at parity (every primitive emits the same extension set for both;
  the inventory verifies this).
