#[cfg(feature = "@{profile_slug}")]
mod @{profile_slug}_values {
    #![allow(non_snake_case)]
    use tsl::tsl_core::*;
    use tsl::tsl_@{profile_slug}::*;
    use tsl::tsl_test_core::*;

@{body}
}
