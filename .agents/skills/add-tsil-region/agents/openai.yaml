interface:
  display_name: "Add TSIL Region"
  short_description: "Add a TSIL region through scan, validate, lower, test."
  default_prompt: "Use $add-tsil-region to add a TSIL region to tslc through scanning, validation, lowering, authoring, and tests."
